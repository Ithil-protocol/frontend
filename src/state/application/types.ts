// `dark` / `light` theme
export type ThemeType = 'dark' | 'light';

// `injected` / `walletconnect` connector, or `none` status
export type WalletConnectorType = 'injected' | 'walletconnect' | 'none';
